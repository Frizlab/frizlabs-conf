---
# Note: If dotfile_files is empty, declare it like so (yaml syntax for empty list):
# dotfile_files: []
dotfile_files:
  - .bash_profile
  - .vimrc

dotfile_templates:
  - .bash_profile:dyn
  - .vimrc:dyn

# vim: ts=2 sw=2 et
