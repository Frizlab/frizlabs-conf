---
- name: "create templates"
  template:
    src: "{{ item | regex_replace('^\\.', '_.') | regex_replace('/\\.', '/_.') }}.jinja"
    dest: "{{ ansible_env.HOME }}/{{ item }}"
    mode: "{{ '0700' if item.endswith('.sh') else '0600' }}"
  loop: "{{ dotfile_templates }}"

# vim: ts=2 sw=2 et
