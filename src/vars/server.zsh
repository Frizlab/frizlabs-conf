# Environment specific vars: server

# Conf
readonly DARK_MODE=false
readonly DEFAULT_BREW_IS_SYSTEM=true

# Custom components
DEFAULT_COMPONENTS_TO_INSTALL+=(
	"homebrew-system" "pkgs-brew-system"
)
