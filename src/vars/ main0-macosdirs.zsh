# The Applications directory.
readonly APP_DIR="$HOME/Applications"
# The Developer directory.
# This is used to put all work that cannot be safely be in iCloud Drive because of sync issues.
# I would like to thank Apple for
#  not allowing to “hide” folders from iCloud Drive other than renaming “NAME.nosync” (an xattr can probably do it now; not sure) and also
#  not allowing to force some files or folder to stay downloaded locally (this is fixed now!).
readonly DEVELOPER_DIR="$HOME/Developer"
